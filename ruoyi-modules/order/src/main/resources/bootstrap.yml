# Tomcat
server:
  port: 8101

# Spring
spring: 
  application:
    # 应用名称
    name: order
  profiles:
    # 环境配置
    active: dev
  cloud:
    nacos:
      discovery:
        # 服务注册地址 公共ip54.210.166.109 私有ip172.31.85.130
        server-addr: 172.31.85.130:8848
      config:
        # 配置中心地址
        server-addr: 172.31.85.130:8848
        # 配置文件格式
        file-extension: yml
        # 共享配置
        shared-configs:
          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
